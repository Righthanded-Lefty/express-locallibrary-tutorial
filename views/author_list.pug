extends layout

block content
  h1= title

  ul
  each author in author_list
    li
      a(href=author.url) #{author.name_MLA}
      | (#{author.dateOfBirth_formatted} - #{author.dateOfDeath_formatted})

  else
    li There are no authors.